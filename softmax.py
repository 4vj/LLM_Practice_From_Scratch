import numpy as np
#outputarray = np.array = []
scores = np.array = [3, 1, -2]
Exponentiate = np.exp(scores)
print(Exponentiate)
total = np.ndarray.sum(Exponentiate)
print(total)
softmax = Exponentiate / total
print(softmax)